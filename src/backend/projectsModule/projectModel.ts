/*
CREATE TABLE projects (
    id INT AUTO_INCREMENT PRIMARY KEY,
    images json NOT NULL,
    title VARCHAR(255) NOT NULL,
    type VARCHAR(255) NOT NULL,
    description TEXT NOT NULL,
    challenge TEXT NOT NULL,
    finalView TEXT NOT NULL,
);
*/
